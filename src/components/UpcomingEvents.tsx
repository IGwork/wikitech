import React from "react";
import "./UpcomingEvents.css";

const UpcomingEvents: React.FC = () => {
  return (
    <div className="upcoming-events">
      <button className="upcoming-btn">Upcoming Events</button>
    </div>
  );
};

export default UpcomingEvents;
